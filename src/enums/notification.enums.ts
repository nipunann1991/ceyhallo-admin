export enum PushNotificationTargetType {
  Topic = 'topic',
  Token = 'token'
}

export enum PushNotificationStatus {
  Pending = 'pending',
  Scheduled = 'scheduled',
  Sending = 'sending',
  Sent = 'sent',
  Failed = 'failed'
}

