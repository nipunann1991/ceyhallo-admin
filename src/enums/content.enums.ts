export enum ActionType {
  WhatsApp = 'whatsapp',
  Call = 'call',
  Email = 'email',
  None = 'none'
}

export enum EventOrganizerType {
  Restaurant = 'restaurant',
  Grocery = 'grocery',
  Business = 'business',
  Organization = 'organization',
  Custom = 'custom'
}

export enum EventActionType {
  External = 'external',
  PaymentLinks = 'payment_links',
  WhatsApp = 'whatsapp',
  Call = 'call',
  None = 'none'
}

export enum HubActionType {
  Link = 'link',
  Call = 'call',
  Email = 'email',
  None = 'none'
}

export enum HubDisplayStyle {
  List = 'list',
  Card = 'card'
}

export enum HubSectionType {
  Service = 'service',
  Emergency = 'emergency'
}

export enum HubSectionDataSource {
  None = 'none',
  Businesses = 'businesses'
}
