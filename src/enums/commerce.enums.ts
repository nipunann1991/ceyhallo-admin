export enum BillingType {
  OneTime = 'one-time',
  Monthly = 'monthly',
  Weekly = 'weekly'
}

export enum MonetizationTargetModule {
  Restaurants = 'restaurants',
  Businesses = 'businesses',
  Events = 'events',
  Jobs = 'jobs',
  General = 'general'
}

export enum TransactionStatus {
  Completed = 'completed',
  Pending = 'pending',
  Failed = 'failed'
}
